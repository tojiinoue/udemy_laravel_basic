[phases.setup]
nixPkgs = ["php", "composer", "nodejs_18"]

[phases.install]
cmds = [
  "composer install",
  "npm install"
]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmd = "php artisan serve --host=0.0.0.0 --port=3000"
